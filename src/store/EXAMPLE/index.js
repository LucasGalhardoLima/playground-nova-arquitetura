/**
 * @example Exemplo de index para o módulo store.
 *
 * import mutations from './mutations';
 * import actions from './actions';
 * import getters from './getters';
 * import state from './state';
 *
 * const state = setInitialState();
 *
 * export default {
 *   namespaced: true,
 *   state,
 *   getters,
 *   actions,
 *   mutations,
 * };
 */
