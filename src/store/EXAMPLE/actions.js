/**
 * @example Configuração do arquivo Actions
 *
 * import types from './types';
 *
 * export default {
 *   actionName: ({commit}, playload) {
 *    commit(types.TYPE_NAME, playload)
 *   }
 * }
 *
 */
